우분투
마리아디비설치
apt -get = 설치목록
update = 목록
upgrade = tpqnsodyd
sudo apt -get install 패키지명 설치
ex)sudo apt -get install mariadb-server
remove 삭제 설정파일을 남겨둔다
purge 삭제 설정파일 같이 제거
list  조건 목록
sudo dpkg -L grep mariadb -마리아디비와 관련된 패키지 확인
sudo apt -get purge -y mariadb =삭제
sudo apt -get install tree
mkdir 파일명 =생성
rmdir 파일명 =삭제
cd 파일명 = 경로
touch(파일만들기) 여러게 가능
도스
rd 디렉토리=삭제
cd 디렉토리=경로
md 디렉토리=생성
와일드 카드문자
*=all
t*=t로시작하는
*t=t로끝나는
t?=t로 시작하는 두글자만 t를포함한다
우분투
cat>파일명 -글쓰기
cat>>파일명 -이어쓰기
cp복사
ps -e 사용하고있는 모든 프로세서가 나온다
kill 사용종료
pid 프로세서 번호
java--환경변수--우선권을 부여 받아 다른프로그램 보다 상위 실행권을 획득하기위해
도스
git config --list = 환경 설정내용
%f 파일이름
etc 설정 파일이 들어가는곳
레파지토리 --저장소
깃 올리는 순서
git config --global user.name"~~"
git config --global user.email"~~"
git init
git status
git add .
git commit -m"~~"
git branch -M main
git remote add origin ~~~~
git push -u origin main
