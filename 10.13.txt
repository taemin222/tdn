 DB
데코드와 필드의 집합
모델링 자료구조를 기호화 한것
고객(자료)---주문(분기  판단)--상품(자료)
1현실성이 있어야함
2단순화(중복회피)
3관리할 데이터를 모델화
모델링시 중점
1.데이터
2.자료처리


PK=프라이머리키
create databases 이르르름; =만드는거
drop database 이르므믐; =없에는거
EX)
create table emp(
empid char(4),  --크기고정
ename varchar(20),  --최대20글자 미만가능
hir_data date, -- 날짜
salary int  ---숫자
primary key (empid)
);

insert (삽입) into 테이블명(필드명)values(데이터값)
update(수정) 테이블명 set 필드이름=데이터값 where 필드이름=데이터값
delete(삭제) from 테이블명 where 필드명= 데이터값
select(선택) 필드명 from 테이블명 where 필드명=데이터값